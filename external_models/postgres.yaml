- name: postgres.public.messages
  description: postgres public messages from generator
  audits:
    - name: not_null
      columns: "[origin_id]"
  columns:
    origin_id: VARCHAR
    event_id: VARCHAR
    message: VARCHAR
    created_at: TIMESTAMPTZ
    inserted_at: TIMESTAMPTZ
    updated_at: TIMESTAMPTZ
